#include <bits/stdc++.h>
using namespace std;

const int N = 2e3 + 1;
const int B = 1e6;
int dx[4] = {1, 0, 0, -1};
int dy[4] = {0, 1, -1, 0};
int f[N][N];
bool vis[N][N];

int main() {
    ios::sync_with_stdio(false);
    cin >> n;
    for (int i = 0; i < n; i++
    string s;
    cin >> s;
    cout << s << endl;
    return 0;
}
