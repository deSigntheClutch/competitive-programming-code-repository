#include <bits/stdc++.h>
using namespace std;

int m, n;

int main() {
  scanf("%d%d", &m, &n);
  vector<int> a(n);
  for(int i = 0; i < n; i++) {
    scanf("%d", &a[i]);
  }

  return 0;
}
