#include <bits/stdc++.h>
using namespace std;

const int N = 1e6 + 5;
const int MOD = 1e9 + 7;

int x;
string str, command;
long long f[N];

int main() {
    ios::sync_with_stdio(false);
    int T;
    cin >> T;
    while (T--) {
  
    }
    return 0;
}