//  g++ -std=c++14 A.cpp -o a && ./a
#include <bits/stdc++.h>
using namespace std;

const int N = 2e5 + 5;

int n, k;
string s, t;

int main() {
    ios::sync_with_stdio(false);
    cin >> n >> k;
    for (int i = 1; i <= n; i++) s += '0' + a[i];
    
    return 0;
}