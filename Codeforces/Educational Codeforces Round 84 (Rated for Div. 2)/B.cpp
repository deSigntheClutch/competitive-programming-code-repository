#include <bits/stdc++.h>
using namespace std;

const int N = 1e5 + 5;

int n;
int G[N];

int main() {
    ios::sync_with_stdio(false);
    int t;
    cin >> t;
    while (t--) {
        cin >> n;
        for (int i = 1 ;i <= n; i++) {
            cin >> len;
            for (int j = 0; j < len; j++) {
                int tmp
                cin >> 
            }
        }
    } 
    return 0;
}